version: "3.8"

services:
  heartbeat-ai:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
      - ./data:/app/data
      - ./outputs:/app/outputs
    environment:
      - LOG_LEVEL=INFO
    restart: unless-stopped
